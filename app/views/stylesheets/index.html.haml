%h1 Stylesheets
%ul
  - for stylesheet in @stylesheets
    %li
      = stylesheet.primary_color
      (
      = link_to 'Generate', generate_stylesheet_path(stylesheet)
      - if stylesheet.last_generated?
        = "Last generated: #{stylesheet.last_generated}"
